<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:extensions="clr-namespace:CHSBackOffice.Extensions"
             x:Class="CHSBackOffice.CustomControls.NoInternetConnectionView">
  <ContentView.Content>
        <Grid BackgroundColor="White">
          <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
                <StackLayout.Resources>
                    <ResourceDictionary>
                        <Style TargetType="Label">
                            <Setter Property="HorizontalTextAlignment" Value="Center"/>
                        </Style>
                    </ResourceDictionary>
                </StackLayout.Resources>
                <Label FontFamily="{StaticResource ChsIcons}" FontSize="{StaticResource Mega}" Text="{StaticResource CHSIconNoInternet}"/>
                <Label Text="{extensions:Translate NoInternetText}" />
          </StackLayout>
        </Grid>
    </ContentView.Content>
</ContentView>